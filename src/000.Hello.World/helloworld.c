#include <stdio.h> //# = 전처리기 (컴파일 전에 미리 처리) ,stdio = standard input output

int main() //프로그램의 시작, int = 정수형 변수
{
	printf("Hello Word \n"); //printf = 괄호안의 내용을 출력, 문자열은 "로 묶음, 1행의 stdio.h 파일을 include하여 사용 가능
	return 0; //0을 반환한다는 의미 main 함수를 호출한 프로그램(운영체제)에게 반환, 정상적으로 종료한다는 의미
}